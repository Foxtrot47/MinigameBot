telethon==1.36.0
aiohttp==3.11.11
python-dotenv==1.0.1

# --- Optional: only needed when OCR_BACKEND=local ---
# Install CPU-only torch first to save ~4 GB:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# Then:
#   pip install easyocr Pillow
